# Create a React-based web application to show a list of posts.

- You can use any library to build UI components.
- Should have clean code & structure.
- Naming of components & variables should be appropriate.

# README.md